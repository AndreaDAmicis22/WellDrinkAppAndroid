<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/first_start_graph"
    app:startDestination="@id/fragment_first_start">
    <fragment
        android:id="@+id/fragment_first_start"
        android:name="com.example.welldrink.ui.FirstStartFragment"
        android:label="First"
        tools:layout="@layout/fragment_first_start">
        <action
            android:id="@+id/action_fragment_first_start_to_fragment_first_start"
            app:destination="@id/fragment_first_start" />
    </fragment>
</navigation>